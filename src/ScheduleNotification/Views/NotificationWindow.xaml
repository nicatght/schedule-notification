<Window x:Class="ScheduleNotification.Views.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reminder"
        Width="350" Height="150"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual">
    
    <Border Background="#FF2D2D30" CornerRadius="8" Padding="15" BorderBrush="#FF007ACC" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="txtTitle" 
                       Grid.Row="0" 
                       FontSize="16" 
                       FontWeight="Bold" 
                       Foreground="White"
                       TextWrapping="Wrap"/>

            <TextBlock x:Name="txtDescription" 
                       Grid.Row="1" 
                       FontSize="12" 
                       Foreground="#FFCCCCCC"
                       Margin="0,5,0,10"
                       TextWrapping="Wrap"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button Content="Complete" Click="Complete_Click" Margin="0,0,5,0" Padding="10,5"/>
                <Button Content="Snooze" Click="Snooze_Click" Margin="0,0,5,0" Padding="10,5"/>
                <Button Content="Dismiss" Click="Dismiss_Click" Padding="10,5"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
